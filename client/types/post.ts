
export type Post = {
	title: string;
	tags: string[];
	content: string;
}

function authenticate(params:type) {
	let auth = false;

	try {
				
	} catch (error) {
		console.error(error);
	}
	return auth;
}
